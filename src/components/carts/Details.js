import React from 'react';

const details = (props) => {
    // const {name, img} = props.player;
    console.log(props);
    return (
        <div>
            {/* <img src={img} alt=""/>
            <h6>Name: {name}</h6> */}
        </div>
    );
};

export default details;